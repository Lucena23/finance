/**
 * SettingsPage.tsx — Configurações do workspace (TAREFA 37 placeholder).
 *
 * Gestão de membros, notificações push e preferências do núcleo familiar.
 * Conteúdo completo nas tarefas subsequentes.
 */

import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

/**
 * Página de Configurações (rota "/settings").
 */
export function SettingsPage(): JSX.Element {
  return (
    <PagePlaceholder
      title="Configurações"
      description="Membros do núcleo familiar, notificações push e preferências do workspace."
    />
  );
}

/**
 * ExpensesPage.tsx — Gestão de despesas (TAREFA 37 placeholder).
 *
 * Lista e formulário de despesas dos três tipos (SINGLE, INSTALLMENT,
 * RECURRENT). Conteúdo completo na TAREFA 42.
 */

import { PagePlaceholder } from '@/components/layout/PagePlaceholder';

/**
 * Página de Despesas (rota "/expenses").
 */
export function ExpensesPage(): JSX.Element {
  return (
    <PagePlaceholder
      title="Despesas"
      description="Cadastro e gestão de despesas avulsas, parceladas e recorrentes."
    />
  );
}
